[project]
name = "gizmo-autonomous-organization"
version = "0.1.0"
description = "GIZMO — Autonomous Intelligence & Development Organization"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
gizmo = "gizmo.core.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
